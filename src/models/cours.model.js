class Cours {
  constructor(id, titre, enseignant, volumeHoraire, actif) {
    this.id = id;
    this.titre = titre;
    this.enseignant = enseignant;
    this.volumeHoraire = volumeHoraire;
    this.actif = actif;
  }
}

module.exports = Cours;
